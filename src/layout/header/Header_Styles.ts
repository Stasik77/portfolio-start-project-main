import { styled } from "styled-components";

const Header = styled.header`
    
    
     position:fixed; 
     padding-top: 25px;
    left:0;
    right:0;
    z-index:9999;
     
    
`  

export const S = {
    Header,
}